@abstract:

(abstract <abstract>)

IEEE abstracts. Works only with IEEE format.

@agradecimentos:

(agradecimentos <agradecimentos do autor1> | <agradecimentos do autor2> | ...)

Agradecimentos dos autores em ordem. Works only with UNB format.

@al-bloc:

(al-bloc <content>)
(al-bloc <title> | <content>)

Alert (red) blocks for presentations.

@appendices:

(appendices)

Start the appendices part in the ieee format.

@autores:

(autores <autor1> | <autor2> | ...)

Autores do formato UNB. Para ser usado antes de `capa` e `inicio`.

@banca:

(banca <prof1> ; <função-prof1> | <prof2> ; <função-prof2> | ...)

Banca de avaliadores do TG UNB. Para ser usado antes de `capa` e `inicio`.

@beg:

(beg <word>)

Beg faz a primeira letra da palavra ficar maior. Só funciona no formato IEEE.

@bf:

(bf <text>)

Text gets bold.xs

@bib:

(bib <ref> | <authors> | <book-name> | <edition>)

Bibliography item. Must be inside `bibliography`.

@bibliography:

(bibliography <bibliography-items>)

Bibliography where items (bib) are put.

@bloc:

(bloc <content>)
(bloc <title> | <content>)

Floating block for presentations.

@capa:

(capa)

Mostra a capa para o formato UNB. Deve ser usado antes de `inicio`

@center:

(center <text>)

Centraliza o conteúdo.

@chapter:

(chapter <chapter-name>)

Chapter.

@cite:

(cite <ref>)

Citation to any label given.

@closing:

(closing <text>)

Closing text for letters. It closes the letter (without it the letter is error)

@codef:

(codef <language> <code>)

Shows code for programs of any language supported around a nice box.

@code:

(code <language> <code>)

Shows code for programs of any language supported.

@color:

(color <color> <text>)

Text is shown with some supported color.

@columns:

(columns <col> | <col> | <col> | ...)

Columns inside a frame (for presentations).

@contents:

(contents)
(contents <depth>)

Table of contents. Depth is a number.

@data:

(data <mes> <ano>)

Data para artigos UNB.

@dedicatorias:

(dedicatorias <titulo1> ; <text1> | <titulo2> ; <text2> | ...)

Dedicatórias para os autores em ordem.

@desc:

(desc <desc1> ; <content1> | <desc2> ; <content2> | ...)

Description topics.

@dots:

(dots -)
(dots |)
(dots \)

Dots for mathematical equations in general. The argument means direction.

@enum:

(enum <topic1> | <topic2> | <topic3> | ...)

Enumerated topics.

@eqn:

(eqn <label> <equation>)

Equation with a label (for references).

@ex-bloc

(ex-bloc <content>)
(ex-bloc <title> | <content>)

Example block (green) for presentations.

@fframe:

(fframe <title> | <content>)

Frame (slides) with fragile option.

@fontsize:

(fontsize <size>)
(fontsize <size> <spacing>)
(fontsize <size> <spacing> <text>)
(fontsize <size> default <text>)
(fontsize <size> def <text>)

Global fontsize (2 first options) or local fontsize (only for <text>). Using <spacing> = def or default sets spacing for 9mm.

@frame:

(frame <title> | <content>)

Frames (slides) for presentation.

@ftframe:

(ftframe <title> | <content>)

Frame with 't' and 'fragile' option.

@grau:

(grau default)
(grau <grau> | <projeto>)

Por exemplo:
Grau: graduando em engenharia ...
Projeto: Iniciação científi...

@hvec:

(hvec [1,2,...])

Horizontal vector.

@imgc:

(imgc <imgFile>)
(imgc <imgFile> <scaleArg>)
(imgc <imgFile> <scaleArg> <caption>)

Centralized image. 'imgfile' is from directory './External/Images' and 'scaleArg' can be 'scale=0.5' or 'width=10cm', etc.

@imgcl:

(imgcl <imgFile>)
(imgcl <imgFile> <scaleArg>)
(imgcl <imgFile> <scaleArg> <label>)
(imgcl <imgFile> <scaleArg> <label> <caption>)

Centralized image with label. 'imgfile' is from directory './External/Images' and 'scaleArg' can be 'scale=0.5' or 'width=10cm', etc.

@img:

(img <imgFile>)
(img <imgFile> <scaleArg>)
(img <imgFile> <scaleArg> <caption>)

Image. 'imgfile' is from directory './External/Images' and 'scaleArg' can be 'scale=0.5' or 'width=10cm', etc.

@imgl:

(imgl <imgFile>)
(imgl <imgFile> <scaleArg>)
(imgl <imgFile> <scaleArg> <label>)
(imgl <imgFile> <scaleArg> <label> <caption>)

Image with label. 'imgfile' is from directory './External/Images' and 'scaleArg' can be 'scale=0.5' or 'width=10cm', etc.

@inicio:

(inicio)

Inicio para o formato 'unb'. É pra ser colocado após todos programas obrigatórios. Depois da 'capa', por exemplo.

@item:

(item <item1> | <item2> | <item> | ...)

Topics with bullets.

@it:

(it <text>)

Make <text> italic.

@letter:

(letter <begginingStatement>)

Beggins a letter with a beggining statement. Its obligatory.

@ltable:

(ltable <length> ; [[1,2,...],[1,2,...],...])
(ltable <length> ; [[1,2,...],[1,2,...],...] ; <label>)
(ltable <length> ; [[1,2,...],[1,2,...],...] ; <label> ; <caption>)

Table with horizontal length argument. Length is like '10cm', for example. Uses tabularx.

@math:

(math <math-text>)

Math ($$..$$). Use this when a program inside math is needed.

@mathi:

(mathi <math-text>)

Math ($..$). Use this when a program inside math is needed.

@mat:

(mat [[1,2,...],[1,2,...],...])

Matrix.

@mc:

(mc <num> <typeOfCell> <text>)

Multicolumn cell for tables. <num> is the number of normal cells the cell ocupies. <typeOfCell> is 'c' for centering, 'l' for left, and it goes on.

@n:

(n)
(n <text>)

'n' alone means a newline in latex (\\) and {n <text>} means that all newlines (\n) transform to newlines in latex (\\).

@opening:

(opening <opening-text>)

Opening for letters. After 'letter'.

@pageref:

(pageref <ref>)

Reference to the page of the label.

@par:

(par <par-name> | <contents>)

Paragraph with a name in front and contents.

@parindent:

(parindent)
(parindent <len>)

Paragraph indentation. Without argument the length of indentation is set to 0pt.

@parskip:

(parskip)
(parskip <len>)

Paragraph skip (vertical space between paragraphs). Without argument the length of skip is set to '\baselineskip'.

@part:

(part <part-name>)

Parts for a partioned document.

@penum:

(penum <item1> | <item2> | ...)

Enumerated topics with pauses. Only for presentation.

@plot:

(plot <code>)
(plot <caption> | <code>)
(plot <label> | <caption> | <code>)

Plot executes a gnuplot code in order to make a graph. Awesome.

@ptable:

(ptable <format> ; [[...]] )
(ptable <format> ; [[...]] ; label)
(ptable <format> ; [[...]] ; label ; caption) 

ptable is table with user given format. The format is like '|c|c|c|' or 'l|l|l'. Does not use tabularx.

@quote:

(quote <text>)

Quotes.

@ref:

(ref <ref>)

Reference to any label. The result is a number.

@resumo:

(resumo <txt>)

Resumo para formato UNB. Não é obrigatório (eu acho).

@rm:

(rm <text>)

Texto com fonte 'roman' (que é a default).

@sc:

(sc <text>)

Texto com fonte 'sc'.

@section1:

(section1 <section-name>)

Section without a number in front.

@section:

(section <section-name>)

Section.

@smallmat:

(smallmat [[...]])

Small matrices for inline math.

@styletitle:

(styletitle <title> )
(styletitle <title> | <author> )
(styletitle <title> | <author> | <date> )
(styletitle <title> | <author> | <date> | <desc> )
(styletitle <title> | <author> | <date> | <desc> | <footer>)
(styletitle <title> | <author> | <date> | <desc> | <footer> | <middle>)

That's it.

@subsection1:

(subsection1 <section-name>)

Subsection without a number in front.

@subsection:

(subsection <section-name>)

Subsection.

@table:

(table [[...]] )
(table [[...]] ; label)
(table [[...]] ; label ; caption) 

Table does not use tabularx.

@tframe:

(tframe <title> | <contents>)

Frame (slide) with 't' (topics) option.

@title:

(title <title>)
(title <title> | <author>)
(title <title> | <author> | <date>)

Title uses maketitle from latex.

@titulo:

(titulo <titulo>)

Titulo para formato UNB. Cada enter vira um newline no latex. Apesar disso, pode-se usar {n} também.

@tt:

(tt <text>)

Text with 'tt' format. Awesome typewriter format.

@vec:

(vec [...])

Vertical vector.

@xtable:

(xtable [[...]])
(xtable [[...]] ; <label>)
(xtable [[...]] ; <label> ; <caption>) 

Xtable uses tabularx.

@multicols:

(multicols <number-of-cols> <text>)

Creates multicolumns for any document.